export const EmptyState = ({ children }) => {
  return (
    <div className="flex flex-col items-center justify-center p-10 w-full border border-white rounded-lg">
      {children}
    </div>
  );
};
